<div class="container">
  
  <form id="submit_comment" name="new_comment" method="post" action="/comment/new/<%= @post.id %>">
    <!-- <input type="hidden" name="post_id" value="<%= @post.id %>"> -->
    <textarea placeholder="Your comment here. Keep it polite." name="comment_text"></textarea>
    <input type="submit" value="share your comment">
  </form>

  <h2>Post</h2>

    Post title: <%= @post.title %></a><br/>
    Post id: <%= @post.id %><br/>
    Post content: <%= @post.url%><br/>
    Created at:<%= @post.created_at%><br/>

  <h2>Comments</h2>
  <% @post.comments.each do |comment| %>
    <%= Post.find(comment.post_id).title%><br/>
    <%= comment.body%><br/>
    <%= comment.created_at%><br/>
  <% end %>

</div>
